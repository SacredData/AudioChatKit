enum FormatConverterSettings {
    static let format = "m4a"
    static let sampleRate = AudioKitSettings.sampleRate
    static let channels = AudioKitSettings.channelCount
    static let bitrate = 64
}
